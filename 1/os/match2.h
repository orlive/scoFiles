#ifndef MATCH2_DEFINE
#define MATCH2_DEFINE

#include <stdio.h>
#include <string.h>

int match(char *str1,char *str2);
int get_klammer(char *str2,int p);
int vergleich(int p1,int p2);
void kill_space(char *str);

#endif
